# Credentials and secrets (never commit)
_credentials/
credentialsMap.json
*.env
.env
.env.*
!.env.example

# Local context / generated
.basicITCS_getAware

# Private notes (hosts, IPs, exports) — see docs/01-overview.md (local-only)
local-only/

# Keys and certs (if any at repo root)
*.pem
*.key

# OS and editor
.DS_Store
Thumbs.db
*.swp
*.swo
*~

# Temp / one-off
temp_nginx/

# Optional
node_modules/
.venv/
__pycache__/
*_temp.txt
